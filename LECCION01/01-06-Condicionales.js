// If simple
if (condition) {
    // Expresion
}

// is else
if (condition) {
    // Expresion 1
} else {
    // Expresion 2
}

// Switch
switch (key) {
    case 1:
        // Expresion 1
        break;
    case 2:
        // Expresion 2
        break;
    case n:
        // Expresion n
        break
    default:
        break;
}