while (condition) {
    // Expresion

    // cortar despues de tantas iteraciones
}

do {
    // Expresion

    // cortar despues de tantas iteraciones
} while (condition);

for (let index = 0; index < array.length; index++) {
    // Expresion
}

array.forEach(element => {
    // Expresion
});